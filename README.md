## Here are all the docker scripts

### How to use

```
$ virtualenv docker-scripts # Make sure repo is cloned to this folder
$ cd docker-scripts 
$ source bin/activate
$ pip install -r requirements.txt
$ fab -l # To get a list of available commands
```

